import type{AnimationTrack}from'../../types/creative';import{evaluateTrack}from'./AnimationRuntime';
export interface CurveSample{time:number;value:number}
export const sampleAnimationCurve=(track:AnimationTrack,start:number,end:number,segments=96):CurveSample[]=>{if(!Number.isFinite(start)||!Number.isFinite(end)||end<start)throw new Error('invalid curve sample range');const n=Math.max(1,Math.min(1024,Math.floor(segments)));const times=new Set<number>();for(let i=0;i<=n;i++)times.add(start+(end-start)*i/n);for(const k of track.keyframes)if(k.time>=start&&k.time<=end){times.add(k.time);if(k.interpolation==='step'){const epsilon=Math.max((end-start)/n/100,1e-7);times.add(Math.min(end,k.time+epsilon))}}return[...times].sort((a,b)=>a-b).map(time=>({time,value:evaluateTrack(track,time)}))};
