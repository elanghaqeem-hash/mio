import type{MioAnimationProject}from'../../types/creative';import type{PoseControlState}from'./PoseControlModel';import{beginPoseDragTransaction,cancelPoseDrag,commitPoseDrag,previewPoseDrag,type PoseDragTransaction}from'./PoseDragTransaction';import{beginTransformGizmoDrag,snapTransformDelta,transformGizmoDelta,type PointerPoint,type ScreenAxis,type TransformGizmoDrag}from'./TransformGizmoController';export interface InteractivePoseSession{gizmo:TransformGizmoDrag;transaction:PoseDragTransaction;snapStep?:number}export const beginInteractivePoseSession=(project:MioAnimationProject,control:PoseControlState,time:number,origin:PointerPoint,snapStep?:number,screenAxis?:ScreenAxis):InteractivePoseSession=>({gizmo:beginTransformGizmoDrag(control.tool,control.axis,origin,80,Math.PI/360,screenAxis),transaction:beginPoseDragTransaction(project,control,time),snapStep});export const updateInteractivePoseSession=(s:InteractivePoseSession,p:PointerPoint):InteractivePoseSession=>{const delta=snapTransformDelta(transformGizmoDelta(s.gizmo,p),s.snapStep);return{...s,transaction:previewPoseDrag(s.transaction,delta)}};export interface InteractivePoseCommit{project:MioAnimationProject;keyedChannels:string[];delta:number}export const commitInteractivePoseSession=(s:InteractivePoseSession):InteractivePoseCommit=>{const c=commitPoseDrag(s.transaction);return{project:c.previewProject,keyedChannels:c.keyedChannels,delta:c.accumulatedDelta}};export const cancelInteractivePoseSession=(s:InteractivePoseSession):MioAnimationProject=>cancelPoseDrag(s.transaction);
