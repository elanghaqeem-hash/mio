import type{MioAnimationProject}from'../../types/creative';import type{BoneTransformChannel}from'./AutoKeyOperations';import{upsertBoneKeyframe}from'./AutoKeyOperations';const index=(axis:string)=>axis==='x'?0:axis==='y'?1:2;
export const setBoneChannelValue=(p:MioAnimationProject,rigId:string,boneId:string,channel:BoneTransformChannel,value:number,time:number,autoKey:boolean):MioAnimationProject=>{if(!Number.isFinite(value)||!Number.isFinite(time))throw new Error('Bone authoring values must be finite');const [group,axis]=channel.split('.')as['position'|'rotation'|'scale','x'|'y'|'z'];const rigs=(p.rigs??[]).map(r=>r.id!==rigId?r:{...r,bones:r.bones.map(b=>b.id!==boneId?b:{...b,pose:{...b.pose,[group]:b.pose[group].map((v,i)=>i===index(axis)?value:v)as[number,number,number]}})});const next={...p,currentTime:time,rigs};return autoKey?upsertBoneKeyframe(next,rigId,boneId,channel,time):next};
