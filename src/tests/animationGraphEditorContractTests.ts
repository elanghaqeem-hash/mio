export async function runAnimationGraphEditorContractTests(){const results:{name:string;passed:boolean;error?:string}[]=[];const run=(name:string,fn:()=>void)=>{try{fn();results.push({name,passed:true})}catch(e){results.push({name,passed:false,error:e instanceof Error?e.message:String(e)})}};run('graph editor module contract',()=>{const required=['AnimationGraphEditor','updateNumericKeyframe','onTimeChange','onCommitProject'];for(const token of required)if(!token)throw new Error('missing graph contract')});for(const r of results)console.log(`${r.passed?'✓':'✗'} [${r.passed?'PASS':'FAIL'}] ${r.name}${r.error?` — ${r.error}`:''}`);return{passed:results.filter(r=>r.passed).length,total:results.length}}
