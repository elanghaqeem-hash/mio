name: Security & Quality Gate

on:
  push:
    branches: [main, 'audit/**', 'security/**']
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci --ignore-scripts

      - name: Dependency vulnerability audit
        run: npm audit --audit-level=high

      - name: Lint
        run: npm run lint

      - name: Security/unit tests
        run: npm test

      - name: Validate Cloudflare Pages Functions
        run: |
          node --check functions/api/capabilities.js
          node --check functions/api/research.js
          node --check functions/api/ai.js

      - name: Web build
        run: npm run build

      - name: Electron TypeScript build
        run: npm run build:electron
