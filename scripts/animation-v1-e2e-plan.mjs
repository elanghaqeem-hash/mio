const steps=[['studio-load','animation-studio-title'],['webgl-viewport','animation-viewport'],['playback','animation-playback-toggle']];console.log(JSON.stringify({suite:'Mio 3D Animation V1 Interactive E2E',requires:{deployedHttpsOrigin:true,realBrowser:true,webgl:true},steps:steps.map(([id,testId])=>({id,testId})),authoringAcceptance:['select rig bone in viewport/outliner','edit pose transform','enable Auto Key and create key','scrub Dope Sheet','inspect/edit F-Curve','create Action','create NLA strip','play/pause and observe evaluated pose','save/reopen and preserve animation','repeat core viewport gestures with touch/pointer'],releaseRule:'productionInteractiveE2E may become true only after all acceptance steps pass against a deployed release origin'},null,2));
