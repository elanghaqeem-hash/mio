import{readFile}from'node:fs/promises';const failures=[];const read=async p=>{try{return await readFile(p,'utf8')}catch{failures.push(`Missing release input: ${p}`);return''}};const entry=await read('src/modes/animation/AnimationStudio3DView.tsx'),workspace=await read('src/modes/animation/AnimationNativeWorkspace.tsx'),manifest=await read('src/modes/animation/AnimationV1Release.ts'),integration=await read('src/tests/animationV1IntegrationTests.ts');if(!entry.includes('<AnimationNativeWorkspace />'))failures.push('3D Animation production entry is not native workspace');for(const token of['MIO_3D_Animation.mioanim','DOPE SHEET','GRAPH EDITOR','ACTION EDITOR','NLA EDITOR'])if(!workspace.includes(token))failures.push(`Native workspace missing ${token}`);if(!manifest.includes('productionInteractiveE2E:false'))failures.push('Release manifest must not overclaim production interactive E2E');if(!integration.includes('evaluateAnimationProject'))failures.push('V1 integration runtime gate missing');if(failures.length){console.error('MIO 3D ANIMATION V1 RELEASE CHECK: FAILED');failures.forEach(x=>console.error(`- ${x}`));process.exit(1)}console.log('MIO 3D ANIMATION V1 RELEASE CHECK: PASS');console.log('Scope: source/build release readiness. Production interactive E2E remains separately required.');
